var Book = /** @class */ (function () {
    function Book(title, author, rating, price, isbn) {
        this.title = title,
            this.author = author,
            this.rating = rating,
            this.price = price,
            this.isbn = isbn;
    }
    return Book;
}());
export { Book };
